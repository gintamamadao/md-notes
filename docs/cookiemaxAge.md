# Parent Node

- [ROOT](./root.md)
- [cookie 属性](./cookie属性.md)

# Child Node

# cookie maxAge

- Cookie 失效的时间，单位秒。如果为整数，则该 Cookie 在 maxAge 秒后失效。
- 如果为负数，该 Cookie 为临时 Cookie，关闭浏览器即失效，浏览器也不会以任何形式保
- 如果为 0，表示删除该 Cookie。默认为-1。
