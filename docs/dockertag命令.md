# Parent Node

- [ROOT](./root.md)
- [docker 命令](./docker命令.md)

# Child Node

# docker tag 命令

- docker tag 用于给镜像打标签，语法如下：

```sh
docker tag SOURCE_IMAGE[:TAG] TARGET_IMAGE[:TAG]
```

- tag 相当于加多一个别名
- push 之前并不是一定要打 tag
