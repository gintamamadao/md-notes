# Parent Node

- [root](./root.md)
- [ECMAScript 隐式转换](./ECMAScript隐式转换.md)

# Child Node

# Detail

- 任意值与布尔值比较，都会将两边的值转化为 Number，除了 null 和 undefined

```js
false == 0; // true
false == []; // [] 先转换为空字串，再转换为数字时为 0, [] == ![] 同理，![] 先转换为 false
```
