# Parent Node

- [root](./root.md)
- [ECMAScript 原型与继承](./ECMAScript原型与继承.md)

# Child Node

# js 原型链

- 对象的 `__proto__` 指向自己构造函数的 `prototype`
- `prototype` 也是对象，也有 `__proto__` 属性
- `prototype` 的 `__proto__` 指向自己构造函数的 `prototype`
- 通过 `__proto__` 这个属性，而由这个属性组成的链，就叫做原型链
